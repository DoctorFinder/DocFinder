import React, { Fragment, useState } from "react";
import {Button} from "react-bootstrap";

export function DoctorStatusComponent() {
    //const [isVerified, setIsVerified] = useState(props.isVerified);
    return (
        <Button>Disabled</Button>
        //<Fragment>
        //    {isVerified && <Button onClick={props.DisableDoctor}>Disable</Button>}
        //    {!isVerified && <Button onClick={props.EnableDoctor}>Enable</Button>}            
        //    </Fragment>
        )
}